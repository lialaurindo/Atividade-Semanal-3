// 6 - crie um algoritmo que converte dias em horas, quando recebe um número de dias.

function converteDiaEmHora(dias) {
    let conversaoDiaHora = dias * 24;
    return (dias) + " dias equivalem a " + (conversaoDiaHora) + " em horas"
}

console.log(converteDiaEmHora(30))
